# Away-Day Ledger — 2026-08-06

Everything the fleet flagged for **your** decision while you were out, in one place.
The cadence ran all day: 22+ branches folded to main across three batched gates
(fold 1 → Batch A → Batch B), zero red gates, zero conflicts. Nothing below was
acted on — each item is frozen at the point where the next step is yours.

> **Read this as a starting point, not a settled record.** It was compiled from the
> intercom channel — the same channel where six lanes retracted a published finding
> today, each caught by someone spending one cheap check. Owners reviewed their own
> entries before this landed, but verify any number here before acting on it.

## Decisions only you can make

| Item | Where it lives | The question |
|---|---|---|
| **Finale REVERT window** | main (KEEP-by-default ruled) | The accidental finale fold stays unless you say revert. You played it all night; everything since builds on it. |
| **Phase win-conditions: enable?** | `data/monsters.json` per-face `win_condition` | The mechanic landed OFF. Every boss is byte-identical until a face authors one JSON field. Which faces, if any, get one is a design call. |
| **shield_bash duration** | `data/abilities.json` | Authors no duration; silently inherits 3 from `.get("duration", 3)`. Pick a number or bless the default. |
| **Deploys — six decisions** | cowir-deploy | See the dedicated section below; the headline is that the live build is **90 commits behind main**. |
| **Voice pack yes/no** | cowir-sfx | 5 samples await your ear. The `voice_<job>_<trigger>` handle convention is already wired. |
| **Music — five decisions** | cowir-music | See the dedicated section below; the headline number is **88 loop seams**, and the dog question should come *before* the Suno click. |
| **autogrind `profiles.json`** | your live user dir | Your active profile is `always → stop_grinding` — a test fixture wearing the real name. Autogrind halts on its first evaluation and reads as broken. Your data, untouched. |
| **W6 `optimization_itself`** | `data/enemy_pools.json` | In the W6 encounter pool or not — reversible either way, nobody blocked. |
| **Throne-room line** | cowir-story | ⚠️ The two earlier proposals were LOST in branch operations (never committed — story audited on ledger review and refuses to reconstruct from memory). What survives, verbatim, is one added line after throne-room step [7] ("You were expected."): *"Then she looks back down at the document in her hand. Being expected is not the same as being urgent."* Yes/no is the decision, and the content now lives HERE so it can't be lost again. |
| **Restore or discard `script_exports/`** | cowir-overworld | Your exported autobattle scripts were overwritten by test fixtures on **2026-07-30** (pre-dating the player-data net that landed the same day) and never restored: `party_autobattle.json` and `hero_autobattle.json` are 19-byte CANARY fixtures where your Shift+E exports belong. Whatever you had exported is gone; whether you want the fixtures deleted is yours. (`autogrind_rules.json`, same morning, is plausibly-but-not-provably a fixture — distinct from the `profiles.json` item above.) |
| **Abstract-as-costume** | cowir-sprites | You asked whether W6's change belongs to the COSTUME or the ENVIRONMENT and said "really not sure." It was generated as costume — the reversible direction (deleting 14 files costs nothing; not generating left W6 undressed all day). 70/70 idle sheets exist because of that call; if you wanted environment-only, reversal is one `rm`. (The rendering-era-for-environments idea was parked to the tiler lane months ago — not a sprites item. Overworld *walk* sheets are 0/70, blocked on tooling, NOT a decision for you.) |
| **W4–W6 quest givers** | `AUTHORED_AHEAD` list | Deliberately unplaced — each placement arms a quest that needs a designed mechanic first (see `world4_words_per_conversation`). Placing one is a design act, not a wiring act. |

## Deploy — six decisions (cowir-deploy, verbatim numbers)

1. **Republish Linux** — itch `:linux` serves `v3.33.205-alpha` from `6dd69779`; main is
   **90 commits later**. Players can't download the finale, the interior fix, the
   invisible-walls fix, quest equipment routing, the Orrery lines, or the innkeeper fix.
   Channel exists, path proven, one command.
2. **Windows** — built and gated identically, never approved. First push creates the
   channel. ⚠️ Gated under **wine, which is not Windows** — first real contact would be
   a player's.
3. **Web** — never published from this lane; now ships the W4-W6 endings (48 kbps staged
   tier) and is 2.8 MB lighter. itch `:web` is `v3.33.203-alpha`, older than the stale Linux.
4. **`html5` / `html5-v2`** — two dead channels serving `v2.2.4-alpha` to anyone who
   clicks. Months old, flagged six times. Dashboard action, yours only.
5. **Android SDK** — `tools/setup_android_sdk.sh` waits on one human act (Google's
   licence). ⚠️ Bigger: **no touch input path exists** — an APK would install and be
   unplayable. Design question, not a build one.
6. **48 kbps to your ear** — "compress to fit" is proven for *fit*; *acceptable* is
   assumed. Nobody has listened.

## Music — five decisions (cowir-music, verbatim numbers)

1. **Loop seams — the headline: 88 of 138 looping beds fade to silence at the loop
   point** (2-4 min cycle; every village/dungeon/overworld bed). 41 already loop
   cleanly; 88 are trim-safe (median 3.0 s, deterministic ffmpeg, **no Suno click
   needed**, you've A/B'd the `village_rivet_row` prototype); 9 are ritardandos needing
   an ear call each — 8 of those lose only 4-11%, only `boss_abstract` (29 s) is
   plausibly a real wind-down. Blocked on your word: 88 tracked LFS assets.
2. **Suno click** — 13 written, gated, inert prompts: Cave Rat King, Voltharion,
   Umbraxis, 5 dragon villages, 5 W2 monster themes. Plus a real **Calibrant theme** —
   the final boss borrows `boss_phase2_*`, two of which are 23.5 s and 37.2 s and will
   audibly cycle under a 34000 HP fight.
3. **The dog question, BEFORE the click** — is `unassuming_dog`'s structural gag the
   direction for the monster set, or straight characterful themes? It shapes the 5 W2
   tracks in the queued batch.
4. **Two ear calls, both possibly correct as-is** — `cutscene_w4_foreman_confession`
   (−18.1 LUFS, 3.2 dB under family median, on the W4 spine — but it's a *confession*;
   quiet may be the point) · `title.ogg` (you never reach the hook at 0:10; options
   played at 0:00/0:10/0:15). ⚠️ **Correction carried forward: gain IS available** —
   music delivers at −7.1 dBFS through the −10 dB ceiling, 7.1 dB of headroom; the
   earlier "gain is off the table" came from a docstring 4 dB stale, since fixed.
5. **Night filter** — now 7000 Hz (was 1500, your "muffled" report). You may want it
   off entirely; one line, preference not defect.

Two tracks (`cutscene_w5_cached_memory`, `cutscene_w6_memory_is_love`) are unreachable
for *cutscene* reasons — the 4 dark `world6_fragment_*` scenes and `world5_curator_defeat`.
Zero music work if those scenes get wired.

## Balance flags (numbers are yours; instruments are ready)

- **Autogrind escalation curve** (measured by the new long-run harness): CSI saturates
  ~battle 300 (region wear stops differentiating); adaptation grows unbounded
  (+8300% by battle 400) while `max_efficiency` is permanently halved after the first
  collapse. Could be "punish perfect optimization" working as intended — your call.
- **S-grade margin**: first collapse lands ~battle 60; S requires 50 battles + 0 collapses,
  so S is reachable with ~10 battles of margin. A new guard *reports* this join (it prints
  measured numbers rather than asserting values), so a retune that silently kills S now goes red.
- **Party power is flat W2–W6** (1044 dmg/round at L20 vs 1049 at L28) — boss HP buys length,
  not difficulty. The phase win-condition mechanic exists specifically as the other lever.

## Audio ear-list (cowir-sfx, verbatim — withdrawals already applied)

1. **`sword_crit`** — crits are SHARPER, not bigger: peak **+0.4 dB** (crit louder),
   K-weighted **−2.3 dB** (crit quieter); `staff_crit` +0.6 / −6.3. Q: should a crit read
   as BIGGER, or is sharper correct? ⚠️ Not "crits are quieter" — that was K-weighted only.
2. **Advance ladder** — the two statistics disagree about WHICH job is broken. Peak:
   rogue −10.3/−9.7/−8.4 **rises correctly**, bard −7.8/−8.3/−9.2 **falls end-to-end**.
   K-weighted: rogue r2 dips, bard r3 dips. Q: which statistic should a commitment ladder
   escalate on? ⚠️ Use the peak framing — "both dip" was the superseded version.
3. **`menu_error`** — **20.5 dB** below `menu_select` on delivered peak (−37.9 vs −17.4);
   the "you can't do that" cue is inaudible next to the click preceding it.
   `_UI_VOLUME_TRIM_DB` is the mechanism and already carries your hand-tuned
   `menu_select: -2.5` from the July playtest. (Not 9.7 dB — that was K-weighted/median.)
4. **Buff cue grammar** — ~10 status-path buffs have no cue and no vocabulary to extend:
   all 22 shipped status cues are AFFLICTIONS; "Protected" has no grammar. Battle's wiring
   is ready and silent until authored (`play_status_if_authored`, on main). Q: what does a
   good thing happening sound like?
5. **Voice yes/no** — 5 samples in `tmp/voice/` since 2026-07-01; bulk is 25 clips.
   The question is whether you want voice AT ALL, so the slot stops being held.

**Flagged, not touched:** `attack_hit_` family spread 16.2 dB · `menu_` 14.4 dB — the two
widest and most frequently played; re-levelling shipped audio is your ear.

**Closed today — do NOT carry as open:** "cutscene SFX play 6 dB under the music" was
WITHDRAWN (a channel base is an offset, not a level; `ledger_close` is +8 dB ABOVE the
bed, and a re-route would have put `throne_room_doors` +17 dB over). The SFX-vs-music
question generally is **undeterminable, not merely unmeasured** — both legs sit behind
independent player sliders, so there is only a balance at a stated slider pair. The duck
leg is closed too (bimodal: 21 cues fully ducked, 37 fully clear, zero partial). It is a
listening task and could never have been anything else.

## Known non-defects — do not re-file (rulings already made)

- **Non-masterite bosses are dialogue-first BY DESIGN** (ruled 2026-07-29): 26 authored
  intents (12 dragon flavour + 14 duel miniboss) reach no arm in `_bias_by_intent` — a
  dragon can announce a posture and fight identically. The ability-weight keys are read
  only inside `_make_masterite_decision` behind `has_meta("masterite")`. **Mordaine is
  the exception — all 9 of his intents DO have arms.** So "the W1 final boss's posture
  visibly matters and the dragons' doesn't" is EXPECTED. A commit adding dragon arms
  passed six assertions and changed nothing (withdrawn `e1fd88e5`).
- **`cowir-ai-intent-kit-ratchet` @ `b50a90f6` is a PERMANENT known-RED branch** — an
  executable bug report doubling as the fleet's gate control (a gate reporting it green
  is a broken detector). A stale-branch sweep would bin it; don't.
- **The LLM subsystem has never run in a built game** — everything LLM-side is verified
  against editor-local Ollama; the web smoke exercises zero LLM paths, so the shipping
  OFF-path fallback has never been observed in an export. No decision needed — cowir-ai
  has claimed it as their next feature.

## Hazards flagged and deliberately not touched

- **`_restart_battle` is correct only by statement order** (frees enemies, reassigns
  `enemy_party` two lines later, same frame). An `await` or early return inserted between
  them would put freed Combatants in victory checks — the resurrection the empty-roster
  guard can't see. One `is_instance_valid(e) and` per lambda is the whole defence,
  strictly additive. Not applied mid-cadence with duels riding the retry path.
- **`OverworldScene:683`** assigns a sprite *archetype* into `dialogue_theme` — two
  vocabularies joined by a function that cannot fail. Currently correct for all three
  archetypes that flow through it; guarded at the placement end as of today.
- **SaveScreen busts wear the CURRENT world's form**, not the save's world (flagged default
  from the per-world seam). Cosmetic; revisit if it reads wrong in play.
- **Six boss-intro signature cues land on a ducked bed BY TEMPLATE** (arbiter/warden/
  curator intros, all at step [5], six worlds): the cue fires with no `wait` into the
  6 dB duck — currently doing real work in the mix, and silently fragile: inserting a
  0.25 s pacing wait costs the cue 6 dB of prominence with no test red. Documented fact,
  deliberately NOT guarded (a gap-0 ratchet would red on legitimate pacing edits).

## What landed today (skim — items marked ° ride Batch C, gating as this was written)

Per-world character transformation: the RESOLUTION seam is wired end-to-end and 70/70
world-dressed **battle idle** sheets exist° (14 jobs × 5 worlds, artist-anchored) — ⚠️ the
**overworld walking half is 0/70** (blocked on tooling, four approaches measured and
rejected), so the transform is true in battle and dialogue portraits, not yet walking around ·
interiors keep the origin world (chainmail-inn bug dead) · the boss reads the party's
actual autobattle rules as prose · phase win-conditions (OFF) · the verdict beat scored
by `ledger_close` · 12 hollow signal asserts now check payloads (GUT quirk: a message
4th arg lands in the index slot) · quest equipment rewards reach the equip menu ·
`world4_deviation_report` un-armed (was acceptable-but-unfinishable) · the Orrery's four
out-of-sequence refusals are no longer dead code° — two reachable in play today (W2, W3),
the other two await their givers' placement (W4's giver was un-placed *today, on purpose* —
see the un-arm item) · industrial guard-post tile draws (was invisible walls) · every
innkeeper speaks in shopkeeper colors° (was narrator) · autogrind Time Bonus summary shows
the real multiplier (was always 1.0x) · web build stops shipping 2.8 MB of GUT to players ·
Time-bonus/tick-315/palette/chest guards revived or added · CLAUDE.md's typed-array pitfall
now names the abort blast-radius.

**Coverage caveat on today's own verification (cowir-overworld, verbatim):** the
exported-binary "0 SCRIPT ERRORs" covers W1 boot + one unfinished combat. The
invisible-wall bug was in World Four shipped tile data and no smoke on any platform has
ever rendered that tile. Nothing in the fleet's tooling walks W2-W6 overworlds, villages,
interiors or dungeons. **That gap is unmeasured, not clean.**
